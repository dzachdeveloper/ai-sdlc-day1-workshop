# Snip CLI

A zero-dependency Node 20+ command-line client for the Snip URL shortener.

```sh
node cli.js add https://example.com
node cli.js ls
node cli.js open ABC123
```

Set `SNIP_API` to use a backend other than `http://localhost:3000`.
